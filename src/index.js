import {
  chart,
  renderer,
  data,
  component
} from './core';
import config from './config';
import './web';
import './q';

/**
 * The mother of all namespaces
 * @namespace picasso
 */

export {
  config,
  chart,
  renderer,
  data,
  component
};
