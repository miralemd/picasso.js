import {
  chart,
  renderer,
  data,
  component
} from './core';
import config from './config';
import q from './q';
import './web';


/**
 * The mother of all namespaces
 * @namespace picasso
 */

export {
  config,
  chart,
  renderer,
  data,
  component,
  q
};
